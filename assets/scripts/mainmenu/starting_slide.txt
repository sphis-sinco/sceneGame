var dummy = defines.DUMMY_SLIDE;
GuiMainMenu.starting_state = "game/intro/narration-0";

if (dummy)
    GuiMainMenu.starting_state = "dummy/dummy";

if (defines.TAIGO && dummy)
{
    trace("TAIGO :D");
    GuiMainMenu.starting_state = "dummy/dummy-taigo";
}
